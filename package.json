{
  "name": "brorecord",
  "version": "0.1.0",
  "description": "Browser extension that records a browser tab and saves as mp4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:acceptance": "playwright test"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0",
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0"
  },
  "dependencies": {
    "mp4-muxer": "^5.2.0"
  }
}
